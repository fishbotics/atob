from setuptools import find_packages, setup

setup(
    name="atob",
    version="0.0.3.1",
    packages=find_packages(),
    install_requires=[
        # 'pybullet',
        # 'numpy',
        # 'pyquaternion',
    ],
)
