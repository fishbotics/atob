from setuptools import setup

setup(
    name="atob",
    version="0.0.2",
    packages=find_packages(),
    install_requires=[
        # 'pybullet',
        # 'numpy',
        # 'pyquaternion',
    ],
)
